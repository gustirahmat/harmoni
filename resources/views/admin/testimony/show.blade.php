@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">Testimony {{ $testimony->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/testimony') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/testimony/' . $testimony->id . '/edit') }}" title="Edit Testimony"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('admin/testimony' . '/' . $testimony->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Testimony" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $testimony->id }}</td>
                                    </tr>
                                    <tr><th> Nama Tamu </th><td> {{ $testimony->nama_tamu }} </td></tr><tr><th> Testimoni </th><td> {{ $testimony->testimoni }} </td></tr><tr><th> Tanggal Trip </th><td> {{ $testimony->tanggal_trip }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
